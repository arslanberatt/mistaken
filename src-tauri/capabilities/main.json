{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Least-privilege capability for the main window: renders/closes the window and allows plain-text clipboard writes only.",
  "windows": ["main"],
  "permissions": ["core:default", "clipboard-manager:allow-write-text"]
}
