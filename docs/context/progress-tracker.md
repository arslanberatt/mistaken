# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

- Spec 01 is implemented and committed. Wave 2 (Specs 02, 03, 05) has been implemented in separate worktrees and merged into `main` at `1334398` (merge order: Spec 02, then Spec 03, then Spec 05); every declared Wave 2 acceptance/verification command passed on the merged tree. Wave 3 (Specs 04, 07, 08) was implemented in separate worktrees (Specs 04/07 from base `40c659e`, Spec 08 from base `6e52978`) and merged into `main` by the integration owner in the order Spec 04, then Spec 07, then Spec 08, landing at merge commit `5fb1934` (see the Wave 3 integration entry below for full evidence). Spec 04's Windows real-hardware evidence follow-up (branch `spec/04-windows-evidence`, base `623e5ab`) was reviewed and merged by the integration owner at `6f2cd47` (see the dedicated entry below). Spec 05 remains `BLOCKED — no candidate approved`; no local ASR model is approved for shipping.

## Current Goal

- Spec 04's Windows real-hardware gap is closed and merged: `spec/04-windows-evidence` (base `623e5ab`) was reviewed and merged into `main` at `6f2cd47` after the integration owner confirmed the monitor self-join fix, the Windows privacy-guidance fix, and the flaky-test fix, then reran the full post-merge check suite (all passing — see below). AC 4/5/7/8/9/10/11/12/13/15 pass on Windows; AC 13 passes with one disclosed sub-clause (replacement-selection after disconnect could not be observed, because Windows keeps a powered-off Bluetooth endpoint enumerable — recorded as a Spec 10 finding, not a Spec 04 defect); AC 14 is **partial** (Mistaken's own process opens zero sockets and persists nothing, but the "networking disabled" clause stays blocked and the embedded WebView2 runtime makes background connections of its own — recorded as a Spec 12 finding). AC 16's high-capability review is this integration-owner review, now closed.
- Spec 05's benchmark evidence remains `BLOCKED` (synthesized-TTS corpus, no `win-x64` host, no candidate clears the fidelity gate). Spec 06 (local ASR microphone transcription) still cannot start: its Spec 04 precondition is now fully satisfied (merged, both-platform real-hardware evidence complete), but its Spec 05 precondition is not. Until Spec 05 has an approved candidate, the next available parallel-safe work is Spec 09 preparation research only (no Spec 09 implementation, since it also depends on Spec 06).

## Completed

- Product name confirmed: `Mistaken`.
- Product purpose defined as raw live English transcription for later Obsidian analysis.
- macOS and Windows are the target desktop platforms.
- Paid/cloud speech APIs were explicitly removed from the product architecture.
- Core functionality must work locally without an account, API key, backend, database, or required internet connection.
- Microphone and system audio will be captured as separate sources.
- Speaker separation rule defined:
  - microphone -> normal transcript line
  - system audio -> `- ` transcript line
- Windows system-audio direction selected: WASAPI Loopback.
- macOS system-audio direction selected: ScreenCaptureKit.
- Primary local ASR runtime to evaluate selected: sherpa-onnx.
- whisper.cpp retained only as an optional local benchmark/fallback candidate.
- Transcript persistence decision finalized: in-memory only; user persists by `Copy All`.
- Grammar correction and rewriting inside Mistaken explicitly excluded.
- Obsidian analysis/editing explicitly separated from the Mistaken desktop app.
- Initial project context documents prepared.
- A 15-spec implementation inventory, dependency graph, safe parallel-terminal waves, worktree protocol, shared-file ownership rules, model assignment, review gates, and future-spec template were defined in `spec-plan.md`.
- Spec 01, `specs/spec-01-repository-desktop-bootstrap.md`, was authored with repository safety, reproducible Tauri 2 tooling, least-privilege capabilities, native launch evidence, and Wave 2 baseline contracts.
- Spec 01 was reconciled with the authoritative UI token set and now owns the shared transcript/runtime types, test harness, Lucide dependency, and write-only Tauri clipboard plugin required to keep Wave 2 worktrees disjoint.
- Spec 02, `specs/spec-02-transcript-domain-workspace.md`, was authored with stable partial/final segment semantics, immutable finals, exact source formatting, in-memory Clear, write-only Copy All, honest unavailable runtime UI, and owned-path verification.
- Spec 03, `specs/spec-03-typed-ipc-runtime-spine.md`, was authored with exact typed commands/events, runtime validation, monotonic status revisions, structured native errors, cancellation-safe listener cleanup, explicit main-window permissions, and real Tauri command/event smoke evidence.
- Spec 01 was reconciled so its Wave 2 baseline explicitly supplies `@tauri-apps/api` and Serde derive support, while Spec 03 is the sole Wave 2 writer for the Tauri entrypoint/build manifest/application permissions/main capability.
- Spec 04, `specs/spec-04-microphone-device-pcm-capture.md`, was authored with real CPAL device selection, explicit macOS/Windows permission behavior, an honest PCM-only microphone test, fixed 100 × 20 ms reusable buffers, allocation-free callback rules, overflow/disconnect recovery, and physical two-platform evidence.
- Specs 01–03 were reconciled around Spec 04: microphone ownership now points to Spec 04, Spec 03 freezes the shared native PCM/error/sink/session contract and microphone activity state, and Spec 02 explicitly yields only its microphone workspace integration surface while preserving transcript behavior.
- Spec 05, `specs/spec-05-asr-benchmark-license-gate.md`, was authored with a 122-clip incorrect-English corpus protocol, committed-manifest/checksum discipline with no audio in Git, a deterministic scorer whose primary metric is mistake preservation rather than raw WER, a pinned candidate set with per-file sizes/SHA-256, an NDJSON adapter-process protocol, numbered fidelity/accuracy/hallucination/latency/resource/size/license gates, and a required explicit approval-or-blocker record.
- Spec 05 ownership was confirmed compatible with Specs 01–04 without edits: it writes only `benchmarks/**` plus a nested `benchmarks/.gitignore`, keeps its harness in a standalone crate with its own manifest/lockfile and an empty `[workspace]` table, and never touches root manifests, lockfiles, `src/**`, `src-tauri/**`, the root `.gitignore`, or context files.
- Spec 06, `specs/spec-06-local-asr-microphone-transcription.md`, was authored with the official version-matched `sherpa-onnx` Rust crate as the recognizer boundary, offline checksum-verified runtime linking, lazy verified model load, a second bounded 30 × 100 ms inference stage, one constant sample rate per recognizer stream, stable segment identity with throttled partials and exactly one immutable final per utterance, verbatim text emission, and both-host spoken-transcription evidence including preserved ungrammatical sentences.
- Spec 07, `specs/spec-07-macos-system-audio-adapter.md`, was authored as a standalone macOS-only crate (`crates/macos-system-audio/**`) with its own manifest/lockfile and no dependency on the application: ScreenCaptureKit audio-only capture with `excludesCurrentProcessAudio`, non-prompting permission inspection plus TCC-gated authoritative availability, runtime audio-format validation, exact 20 ms mono blocks with drop-newest accounting, idempotent leak-free teardown, a frozen error taxonomy mapped one-to-one onto Spec 03 for Spec 09, and macOS 13.0 frozen as the minimum version.
- Spec 08, `specs/spec-08-windows-system-audio-adapter.md`, was authored as a standalone Windows-only crate (`crates/windows-system-audio/**`) mirroring Spec 07's shape: event-driven WASAPI loopback on the default render endpoint, mix-format validation with explicit float/16/24/32-bit conversion branches, a continuous timeline that fills measured idle gaps with capped silence and counts captured versus synthesized frames separately, once-per-second default-endpoint change detection, COM scoped to the adapter's own thread, no permission API because Windows has no gate for render loopback, and Windows 10 1703 API floor with 22H2/Windows 11 as the tested floor.
- Spec 09, `specs/spec-09-dual-source-transcription-aggregation.md`, was authored as the single-writer Wave 5 integration: both platform crates registered target-specifically with their mapping tables implemented exhaustively, three allowed source combinations with atomic start, two fully independent pipelines (2 s + 3 s bounded stages each, separate streams/workers/indices) over one shared `Send + Sync` recognizer, one session clock origin with per-source fed-frame timestamps, emission-order cross-source ordering with the reorder window explicitly rejected, per-source status/activity/errors with survivor-continuity on single-source failure, and scored zero-cross-attribution evidence from simultaneous speech plus known playback on both hosts.
- Spec 10, `specs/spec-10-capture-lifecycle-resilience.md`, was authored with a frozen per-source recovery policy (3 attempts, 500 ms/2 s/5 s backoff, 60 s healthy reset, no retry for permission/platform/model/internal), recovery-safe identity rules that continue the segment index and clamp the restarted offset so Spec 02's reducer never rejects a replacement, fresh recognizer streams per attempt against the fatal rate-change constraint, visible-only sustained-lag degradation with zero auto-mitigation, panic and poisoned-lock containment, watchdog-bounded transitions, one idempotent shutdown from window close/exit/signals, and a 60-minute dual-source soak with flat-resource evidence on both hosts.
- Spec 11, `specs/spec-11-desktop-interaction-accessibility.md`, was authored as the Wave 6 frontend owner: platform-correct `Cmd/Ctrl + Enter` Start/Stop and `Cmd/Ctrl + Shift + C` Copy All shortcuts with handled-only key suppression, a 64 px near-bottom auto-follow contract with detached-scroll stability and `Jump to latest`, wall-clock-derived elapsed time, named `role="log"` semantics with interim subtrees excluded from live announcements and finals announced once, bounded listener/timer cleanup, focus/contrast/text-scaling requirements, and measured single-row rerender isolation at roughly 1,000 finalized segments on both hosts.
- Spec 12, `specs/spec-12-offline-privacy-performance-acceptance.md`, was authored as the one-writer Wave 7 acceptance owner: a release-profile offline build and complete real native flow on both reference hosts, two-layer no-route plus process-tree attempt detection, content-aware transcript/audio persistence inspection with the explicit clipboard boundary, final-SHA fidelity/latency/RTF/CPU/RSS/overflow/one-hour-soak gates, integrated VoiceOver/Narrator and long-transcript checks, closed PASS/FAIL/BLOCKED evidence, and the version/model/resource/notice/lock freeze required by Specs 13–14.
- Spec 13, `specs/spec-13-macos-packaging-distribution.md`, was authored as the Wave 8 macOS packaging owner: Apple Silicon-only direct distribution through a standard `.app` plus drag-to-Applications DMG; frozen Spec 12 model/runtime/notices embedded once; exact microphone purpose text with no guessed Screen Recording key; non-App-Store/non-sandboxed hardened-runtime packaging with an empty entitlement baseline; Developer ID signing, secure timestamp, `notarytool`, stapling, Gatekeeper, quarantined Finder install, offline installed core flow, normalized two-build reproducibility, real macOS 13.x floor evidence, and precise credential/service/host blockers.
- Spec 14, `specs/spec-14-windows-packaging-distribution.md`, was authored as the Wave 8 Windows packaging owner mirroring Spec 13: x64-only `x86_64-pc-windows-msvc` distribution on the declared Windows 10 22H2/11 floor; exactly one per-user NSIS `Mistaken_0.1.0_x64-setup.exe` with no elevation, service, startup key, or driver; the embedded Evergreen WebView2 offline installer so installation needs no internet; `staticVCRuntime` with no redistributable or virtual audio device; a digest-pinned online tool/runtime staging phase separated from the traced offline measured build; SHA-256 Authenticode signing with an RFC 3161 timestamp verified by `signtool verify /pa /all`; honest SmartScreen/Defender observation with no bypass; real Windows 11 plus Windows 10 22H2 floor runs; per-user install/registry/shortcut/uninstall residue accounting with WebView2 classified as retained OS state; normalized two-build reproducibility; and precise credential/host blockers.
- Spec 15, `specs/spec-15-cross-platform-release-acceptance.md`, was authored as the terminal Wave 9 release gate with one serialized writer who is also the integration owner: one release candidate identity (one release SHA, version `0.1.0`, exactly two signed artifacts `Mistaken_0.1.0_aarch64.dmg` and `Mistaken_0.1.0_x64-setup.exe`); a predecessor consistency contract requiring Specs 13 and 14 to share one post-Spec-12 base SHA; payload reconciliation with a closed documented platform-difference set; installed-artifact reverification of the full user flow, Spec 12 source-separation corpus, and Spec 05 fidelity/latency/resource gates on clean accounts offline on four hosts including both floor hosts; cross-platform parity of transcript output, controls/states/shortcuts, accessibility, privacy, lifecycle, and removal; one installed 60-minute dual soak per platform; a separate closed `release/schema.json` plus manifest, checksums, traceable notes, a rehearsed no-updater rollback checklist, and a publication gate that uploads nothing without a user-supplied destination; documentation reconciliation across all specs and context files; and one closed `RELEASE-READY` / `NOT-RELEASE-READY` / `BLOCKED` verdict with a locally created annotated `v0.1.0` tag.
- Spec 01 was implemented: `/Users/berat/mistaken` is a new `main`-branch Git repository scaffolded from the current official `create-tauri-app` Tauri 2 React TypeScript npm template, with the generated `greet` command, `invoke("greet")` call, opener plugin/capability, template logos/CSS, and sample assets fully removed. The official clipboard-manager plugin is the only registered feature plugin, capped to `clipboard-manager:allow-write-text` in the `main` capability alongside the `core:default` permission evidenced as required to render/close the window; Tailwind CSS is wired through `@tailwindcss/vite` with the shared `ui-context.md` token set defined once in `src/index.css`; `src/types/transcript.ts` and `src/types/runtime.ts` freeze the Wave 2 `TranscriptSource`/`TranscriptSegment`/`CaptureStatus` contracts; Vitest + jsdom + Testing Library exercise one honest bootstrap-screen behavior test; Rust is pinned via `rust-toolchain.toml` to the `rustup`-installed stable toolchain with `rustfmt`/`clippy`; and the canonical `docs/context/**` (six context files plus `spec-plan.md`) and `docs/specs/**` (all 15 reviewed specs) are committed byte-identical to the `/Users/berat/mistaken-context` staging bundle. `AGENTS.md` defines the required read order, spec/worktree workflow, verification obligations, and the BMAD/cloud/API-key ban.
- Spec 01 verification evidence: `npm ci`, `npm run typecheck`, `npm run lint` (oxlint), `npm run build`, and `npm test` (one passing Vitest bootstrap-screen assertion) all passed from a clean install; `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, and `cargo check` all passed from `src-tauri`; `npm run tauri build -- --debug --no-bundle` produced `src-tauri/target/debug/mistaken`; `npm run tauri dev` opened a real native window titled `Mistaken` at the configured `1040×720` initial size and centered position, showing only `Mistaken`, `Desktop runtime ready`, and `Audio capture and transcription are not configured yet.` with no working-looking control; the window was resized to the configured `720×520` minimum with the surface remaining readable and unclipped; the app was closed and relaunched with the dev port and native process cleanly released between runs; the only established network connection observed during the run was the webview's own loopback connection to the local Vite dev server, with zero non-loopback connections from the native process or its WebKit helper; no OS permission prompt appeared; and no transcript/database/model/recording/telemetry file was written by the application itself (the only filesystem artifacts created were WebKit's own standard local `NetworkCache`/`WebsiteData` engine directories under `~/Library/Caches/mistaken` and `~/Library/WebKit/mistaken`, inherent to any WKWebView-based app and containing no transcript or product content — classified rather than denied, consistent with the boundary Spec 12 later formalizes). Required copy contrast was computed against the actual tokens: `--text-primary` on `--bg-base` is ~17.7:1 and `--text-secondary` on `--bg-base` is ~11.5:1, both well above the WCAG AA 4.5:1 threshold; the bootstrap surface uses only semantic `header`/`main`/`h1`/`p` elements with no focusable decorative element and no animation.
- Spec 02 was implemented and merged into `main` (merge commit `19e8942`): the single-window transcript workspace, deterministic partial/final segment reducer, exact `- ` system-source formatting, in-memory Clear with confirmation, and write-only final-only Copy All.
- Spec 03 was implemented and merged into `main` (merge commit `40c659e`): the four typed commands (`get_runtime_snapshot`, `list_microphones`, `start_capture`, `stop_capture`), the six frozen native events, monotonic-revision runtime state, structured `RuntimeError` codes, cancellation-safe listener cleanup, and the narrowed `main` capability.
- Spec 05 was implemented and merged into `main` (merge commit new tip `1334398`): the 122-clip (130 manifest entries including the 48 kHz duplicate subset) corpus manifest/protocol/prompts, the `mistaken-bench` Rust harness (adapter process protocol, WER/MPR/insertion scoring, numbered gates, reporting), the sherpa-onnx and whisper.cpp adapter sources, the license record, and `benchmarks/reports/approval.md`. The committed README discloses that this implementation's corpus audio was synthesized locally with macOS `say` TTS rather than recorded from a human speaker per the protocol's consent requirement, so `approval.md` records this as a blocking limitation: no candidate is approved from this evidence, and a real approval requires a re-run with real human recordings.
- Wave 2 merge verification, run against the fully merged `main` tree (`npm ci` fresh install): `npm run typecheck`, `npm run lint` (oxlint), `npm run build`, and `npm test` (87 passing Vitest tests across 5 files) all passed; from `src-tauri`, `cargo fmt --check`, `cargo check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test` (27 passing tests) all passed; from `benchmarks/harness`, `cargo fmt --check` and `cargo test` (70 passing tests) passed. `mistaken-bench validate-corpus` fails with 130 missing-clip-file errors, which is expected and by design: `benchmarks/corpus/clips/**` is Git-ignored and this checkout has no local audio, not a merge defect.
- Post-merge cleanup: the `mistaken-spec-02`, `mistaken-spec-03`, and `mistaken-spec-05` worktrees and their branches were removed (`git worktree remove` + `git worktree prune` + `git branch -d`) now that their commits are merged and this evidence is recorded. The `mistaken-spec-04` and `mistaken-spec-07` Wave 3 worktrees, already provisioned from the pre-Spec-05 `40c659e` integration point, were left untouched since Spec 05 is disjoint from Wave 3 and per `spec-plan.md` did not need to block their start.
- **Wave 3 integration (Specs 04, 07, 08) — merged into `main`, integration owner review.** All three branches stayed inside their declared ownership boundaries (verified by `git diff --stat` against each branch's actual merge-base, not the label in its own evidence record): Spec 04 touched only its owned root-level files (`src-tauri/src/audio/**` additions, focused `commands/runtime.rs`/`state/*.rs`/`events.rs`/`lib.rs` edits, `Info.plist`, `Cargo.toml`/`Cargo.lock`, `src/features/audio/**`, focused `App.tsx`/`TranscriptWorkspace.tsx` edits); Spec 07 touched only `crates/macos-system-audio/**`; Spec 08 touched only `crates/windows-system-audio/**`. Zero cross-contamination among the three, confirming the frozen Wave 3 ownership split held in practice.
  - **Base-SHA discrepancy found and reconciled:** Specs 04 and 07 were provisioned from the pre-Spec-05 base `40c659e` (confirmed by `git merge-base`), not the `1334398`/`6e52978` SHA the current-phase note names, because their worktrees were created before Spec 05 merged. This was safe to merge as-is because Spec 05 only ever touched `benchmarks/**`, which neither branch touches — verified by inspecting each branch's full changed-path list before merging, not assumed.
  - **Spec 04 merged (commit `4c46de1`):** real macOS evidence (Apple M4, macOS 15.7.5) is solid — real TCC grant flow with the built bundle's actual `Info.plist` string, `waiting → receiving` PCM signal transition, 6 consecutive Start/Stop cycles with flat thread count and ~2 MB RSS drift (webview noise, not a pool leak), clean active-close/relaunch. **Genuine, disclosed blocker, not fabricated:** the implementing session had zero Windows hardware access, so AC 7 (Windows privacy behavior) and the Windows halves of AC 4/5/8/11/12/13/14/15 are entirely unverified — the Windows CPAL code path only compiles under `cargo check`'s type-system reasoning, it has never run. AC 13 (physical microphone disconnect) is also blocked (no removable microphone on the verification host). Only the `docs/context/progress-tracker.md` conflict from the stale base was resolved by keeping `main`'s content; every other file merged cleanly.
  - **Spec 07 merged (commit `795f9b6`):** standalone crate, zero shared-file edits. Real macOS evidence (Mac mini, Apple M4, macOS 15.7.5): 48 kHz/2-channel capture validated against real playback (`afplay`), 5 consecutive Start/Stop cycles with flat thread count and diminishing (not linear) RSS growth, silence-vs-no-audio distinguished, audio-only guarantee confirmed by source grep (zero image/pixel/screenshot API references, zero files written). Disclosed gaps: the live permission-denied path and `PermissionRequiresRestart` were not exercised (host already had a standing grant) and a refusing-sink drop was not exercised against a real ScreenCaptureKit buffer (only unit-tested) — both are recorded, not claimed passed.
  - **Spec 08 merged (commit `5fb1934`):** standalone crate, zero shared-file edits. Commit `23af4a6` adds **real** Windows hardware evidence (MONSTER ABRA A5 V17.2, Intel i5-11400H, Windows 10 Education build 19045 — the crate's documented tested floor): 48 kHz Float32 loopback of a real 440 Hz tone verified sample-accurate (RMS within 0.3% of theoretical), exactly 50 blocks/s over 5,000+ blocks with zero out-of-range samples, 6 Start/Stop cycles with flat handle/thread counts after cycle 1 warm-up, a slow-sink drop-accounting run showing exact production/consumption drop math, zero open sockets during capture. **Genuinely BLOCKED on real hardware** (disclosed, not fabricated): the idle-timeline silence-synthesis path and long-gap cap were never exercised because the host's audio engine never went idle (other applications held continuous render streams); default-endpoint switch, device invalidation, and `AudioServiceDown`/`NoRenderEndpoint` induction all require Administrator rights the verification session did not hold, or disrupting a live host session — none were performed. All of these are covered by synthetic unit tests but not a live hardware signal. **Toolchain gap:** the Windows verification build used `x86_64-pc-windows-gnu` (MinGW); the crate has never been built for the `x86_64-pc-windows-msvc` target the application actually ships on — an open item before Spec 09/14 rely on it.
  - **Post-merge combined checks, run against the fully merged `main` tree (fresh `npm ci`):** `npm run typecheck`, `npm run lint` (oxlint), `npm run build`, and `npm test` (107 passing Vitest tests across 7 files) all passed; from `src-tauri`, `cargo fmt --check`, `cargo check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test` (55 passing tests) all passed; from `crates/macos-system-audio`, `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test` (14 passing tests), and `cargo build --all-targets` all passed; from `benchmarks/harness`, `cargo fmt --check` and `cargo test` (70 passing tests) passed, confirming Wave 3 caused no regression to Spec 05's harness. `crates/windows-system-audio` intentionally fails `cargo check` on this macOS host with an explicit `compile_error!` naming the spec section — this is the designed AC2 behavior (a Windows-only crate must refuse to compile a stub on another target), not a defect.
  - **Not accepted as complete, carried forward:** Spec 04's Windows real-hardware verification and Spec 08's Administrator-gated/idle-timeline scenarios remain open before either spec can be called fully done against its own Definition of Done. Neither gap blocks merging the real, working code already produced (the same precedent Spec 05 set: a disclosed structural blocker does not mean the artifact is discarded), but both gaps independently block Spec 06/Spec 09 from claiming their predecessor evidence is complete.

- **Spec 04 Windows real-hardware evidence (follow-up, branch `spec/04-windows-evidence`, base `623e5ab`, 2026-09-14).** Exercised the already-merged microphone implementation on a physical Windows 10 Education 22H2 host (build 19045.6456; MONSTER ABRA A5 V17.2; Intel i5-11400H) against a built-in `Mikrofon (Realtek(R) Audio)` (F32 48 kHz stereo) and a removable `JBL TUNE660NC` Bluetooth microphone (F32 16 kHz mono), driving the real Tauri UI. The implementation was **not** redesigned; only two defects and one flaky test were fixed.
  - **Windows checks, all native, all exit 0:** `cargo fmt --check`, `cargo check --locked --all-targets`, `cargo clippy --locked --all-targets --all-features -- -D warnings` (zero warnings), `cargo test --lib` 56/56, plus `npm run typecheck`, `npm run lint`, `npm test` 109/109, `npm run build`.
  - **Defect 1 — real Windows deadlock, fixed.** `RuntimeMonitorObserver::on_fault` drops the `MicrophoneMonitor` on the monitor thread itself, and `request_stop_and_join` then joined the current thread. On Windows that never returns, hanging the monitor while it holds the session mutex and wedging the whole capture lifecycle on any device loss — exactly what AC 12 forbids. Reproduced with a new regression test that failed on the pre-fix code, then fixed by detaching instead of self-joining.
  - **Defect 2 — missing Windows privacy guidance, fixed.** A real machine-wide microphone denial surfaces from CPAL/WASAPI as `capture_start_failed` (CPAL does not map `E_ACCESSDENIED`), but the UI only rendered settings guidance for `microphone_permission_denied`, so the actual Windows denial path showed none. AC 7's mandated Settings navigation now accompanies `capture_start_failed`/`microphone_unavailable` on non-macOS, with the spec's exact copy unchanged.
  - **Honest gaps, not marked PASS:** AC 14 is partial (the "networking disabled" clause was not exercised — it would have cut the session driving the verification — and the embedded WebView2 runtime opens background HTTPS connections Mistaken's own code never requests, while `mistaken.exe` itself held zero sockets). AC 13 passes but its "Refresh selects an available replacement" sub-clause could not be observed, because Windows keeps a powered-off Bluetooth capture endpoint enumerable. AC 16's external high-capability review is still outstanding. Real queue overflow was never induced on Windows and is not claimed.
  - **Merged.** Reviewed by the integration owner against real Windows evidence, the self-join fix, the privacy-guidance fix, and the flaky-test fix; merged into `main` at `6f2cd47` (merge commit, base `623e5ab`, `git merge --no-ff`). Post-merge combined checks (`npm run typecheck`, `npm run lint`, `npm run build`, `npm test` — 109 passed; from `src-tauri`: `cargo fmt --check`, `cargo check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test` — 56 passed, including the new deadlock regression test) all passed on the macOS integration host. The self-join fix was independently verified by the integration owner: temporarily reverting it reproduces a real failure on macOS too (`pthread_join` returns `EDEADLK`, panicking the monitor thread), confirming the regression test has teeth on both platforms even though only Windows silently hangs instead of erroring. Two host-level build workarounds (GNU-toolchain cdylib export limit; missing comctl32 v6 manifest on cargo test binaries) were used without changing any repository build configuration, and are handed to Spec 14.

## In Progress

- Wave 3 (Specs 04, 07, 08) is merged into `main` and clean per the combined checks above. Spec 04's Windows evidence gap is now closed and merged (`6f2cd47`). Spec 05 remains blocked; part of Spec 08's evidence remains outstanding follow-up verification, tracked above rather than re-opened as new work.

## Next Up

1. Obtain a human-recorded benchmark corpus (real speakers, informed consent per `corpus/protocol.md`) and a `win-x64` host meeting Spec 05's minimum profile, then re-run the full Spec 05 gate set; only a passing re-run can approve a candidate.
2. Spec 04's Windows evidence branch is merged; no further action needed here. Its disclosed follow-ups belong to other specs: the Windows toolchain/link workarounds → Spec 14; the WebView2 background connections and the un-exercised "networking disabled" clause → Spec 12; a powered-off Bluetooth endpoint staying enumerable → Spec 10; and §7's inaccurate "Refresh/retry can recover" sentence for Windows (the OS caches desktop-app microphone consent per process, so only a relaunch recovers) → a spec-text correction for the integration owner to make before Spec 06 relies on that wording.
3. When feasible, complete Spec 08's Administrator-gated and idle-timeline scenarios (`NoRenderEndpoint`, `AudioServiceDown`, `DeviceInvalidated`, `EndpointChanged`, real silence-gap synthesis) and a native `x86_64-pc-windows-msvc` build/test pass, recorded as a follow-up evidence update to `specs/spec-08-windows-system-audio-adapter.md`.
4. Spec 06 (local ASR microphone transcription) does not start until both Spec 05 has an approved candidate and Spec 04's real two-platform microphone evidence is complete and merged. Spec 04's condition is now fully met (merged at `6f2cd47`); Spec 05's condition still does not hold.
5. Spec 09 may start only after Specs 06, 07, and 08 each pass real-hardware verification, high-capability review, and merge; Spec 07 and Spec 08's core adapters are merged, but Spec 06 is not yet unblocked (see above), so Spec 09 has no available start condition yet.
6. Continue only through the dependency waves defined in `spec-plan.md`; never place two writers in one physical checkout.

## Open Questions

- Which exact English ASR model should ship as the default after Mistaken-specific benchmarking? Spec 05 decides this only from measured evidence; no candidate is preselected, and none has passed the fidelity gate yet on any corpus.
- Windows desktop microphone privacy setting effect on render-endpoint loopback: **partially answered.** Spec 08's real-hardware run found the setting at `Allow` throughout, with no prompt at any point; the negative direction (setting it to `Deny` and confirming loopback is unaffected) was not exercised.
- Does the Windows reference host meet Spec 05's minimum benchmark profile (x64, ≥ 8 cores, ≥ 16 GB RAM, AC power)? The Spec 08 verification host (Intel i5-11400H, 6 cores/12 threads, 15.8 GB RAM) does not meet the ≥ 8-core floor; a separate qualifying host is still needed for Spec 05.

## Architecture Decisions

- **Tauri 2 + React + TypeScript**: provides a cross-platform desktop shell while allowing native Rust integration for local audio and ASR.
- **Rust native core**: audio capture and local inference need predictable native resource control and should not run in the webview.
- **No paid/cloud ASR**: core transcription must have zero per-minute transcription cost and no API-key dependency.
- **Local ASR abstraction**: prevents the whole app from becoming coupled to one model/runtime and allows accuracy benchmarking.
- **sherpa-onnx first evaluation target**: supports native offline speech-recognition workflows and related local speech tooling.
- **whisper.cpp as benchmark/fallback candidate**: useful local comparison path, but not automatically selected as the primary recognizer.
- **Model selection by benchmark, not assumption**: Mistaken requires preservation of incorrect English, so generic ASR reputation alone is insufficient.
- **Separate model-license review**: an open-source runtime does not automatically imply every model weight can be redistributed under the same terms.
- **WASAPI Loopback on Windows**: native Windows path for system-output capture without requiring a paid service or virtual cable as a core dependency.
- **ScreenCaptureKit on macOS**: native Apple path for system-audio capture.
- **Source-based speaker separation**: microphone/system source metadata is more reliable than diarization for the normal two-person workflow.
- **Two independent ASR sessions**: microphone and system audio are never mixed before transcription.
- **In-memory transcript only**: avoids building a persistence system that is unnecessary for the user's workflow.
- **No grammar correction in app**: the transcript is evidence of the user's spoken English and must not be intentionally cleaned before Obsidian analysis.
- **Obsidian is external**: Mistaken outputs copyable plain text and does not need vault access.
- **Read/copy-only V1 transcript**: users may select and copy transcript text, but manual editing is out of scope.
- **Clear confirmation boundary**: `Clear` requires confirmation whenever finalized transcript content exists; interim-only content may be cleared directly.
- **Final-only Copy All**: clipboard serialization excludes unstable interim segments, preserves first-seen final order, adds `- ` only from structural system-source metadata, and performs no text normalization or correction.
- **Parallel delivery by dependency and path ownership**: 15 specs run in separate Git worktrees only when their predecessors are merged and their exact owned paths do not overlap; one integration owner controls shared files and context updates.
- **High-author, tiered implementation**: high-reasoning models author/finalize every spec; medium models may implement deterministic bounded units, while native audio, ASR, licensing, real-time lifecycle, acceptance, and final integration stay high-model work by default.
- **Typed runtime boundary:** Four application commands (`get_runtime_snapshot`, `list_microphones`, `start_capture`, `stop_capture`) and six main-webview native events form the reviewed shared spine; values are runtime-validated, status ordering uses monotonic revisions, and structured error codes—not message text—drive behavior.
- **Truthful pre-backend behavior:** Before audio/model specs land, snapshot reports unavailable resources and operational commands reject predictably without fake devices, fake success, or native resource allocation.
- **Listener and permission containment:** Frontend listeners register before snapshot, clean up asynchronous/partial registration safely, and receive small JSON events only; the `main` capability grants exact application commands and listen permission but no frontend emit or wildcard access.
- **Cross-platform microphone adapter:** CPAL `0.18.2` with stable device IDs is the reviewed V1 microphone I/O boundary; ASIO, JACK, browser media, virtual drivers, and cloud capture are excluded.
- **Bounded PCM handoff:** Microphone capture downmixes to native-rate mono `f32` in preallocated 20 ms blocks; exactly 100 reusable blocks bound queued PCM to two seconds, and callback overflow drops newest frames without blocking or allocation.
- **Honest pre-ASR test:** Spec 04 exposes a separate local `Test microphone` flow and keeps production `Start Listening` disabled until Spec 06 provides approved ASR; no PCM or continuous level crosses IPC.
- **Microphone permission timing:** macOS authorization is checked/requested only from explicit Test using the bundled usage description; Windows desktop privacy denial is reported from observable native behavior and never inferred from silence.
- **Wave 3 shared ownership:** Spec 04 is the sole Wave 3 writer for root Cargo/runtime/UI microphone integration; Specs 07–08 keep platform system-audio implementations and manifests isolated until Spec 09 performs shared wiring.
- **Fidelity outranks WER:** Model approval is driven first by mistake-preservation rate over annotated error spans (≥ 0.90, with ≤ 0.05 false-correction rate) and only then by word error rate (≤ 0.25 corpus, ≤ 0.12 fluent control); a lower-WER model that silently fixes grammar loses to a higher-WER model that preserves it.
- **Frozen Spec 05 gates:** Latency ≤ 900 ms median first partial and ≤ 1500 ms p95 final-after-endpoint; RTF ≤ 0.6 single-stream and ≤ 0.9 dual-stream; peak RSS ≤ 700 MB per stream and ≤ 1.4 GB for two; zero non-empty output on physical-silence clips; model payload ≤ 120 MB uncompressed for bundling. Lowering a gate requires a recorded product decision, not a spec edit.
- **Reference benchmark hosts:** `mac-arm64` (Apple Silicon, ≥ 8 cores, ≥ 16 GB, AC power; the M4 development host qualifies) and `win-x64` (≥ 8 cores, ≥ 16 GB, AC power). Both must pass every gate; one host's result never substitutes for the other's.
- **Pinned candidate set:** sherpa-onnx streaming Zipformer `en-2023-06-26` int8/fp32 (left-64) and `en-20M-2023-02-17` int8, plus whisper.cpp `base.en`/`small.en` ggml as non-streaming comparisons; runtimes are built from exact release tags and every model file is pinned by byte size and SHA-256. The `en-2023-06-21` variant is excluded because its GigaSpeech training terms are unresolved.
- **Separate license and provenance verification:** sherpa-onnx is Apache-2.0, ONNX Runtime and whisper.cpp are MIT, and the candidate model cards declare Apache-2.0/MIT, but the `2023-06-26` upstream TorchScript repository declares no license and LibriSpeech is CC BY 4.0 with an attribution duty; an unresolved provenance chain blocks approval regardless of accuracy.
- **No audio or weights in Git:** The benchmark corpus commits prompts, protocol, verbatim references, error-span annotations, and per-clip checksums, while recordings, model files, vendored runtime sources, and run hypotheses stay local and ignored; speakers are opaque profile ids with a consent flag and no demographic data.
- **Honest streaming comparison:** whisper.cpp has no native streaming recognizer, so its adapter reports `simulated-rolling-window` and its latency numbers are never presented as streaming latency.
- **Official version-matched recognizer binding:** The shipped ASR boundary is the `sherpa-onnx` Rust crate published in lockstep with the C++ project (`sherpa-onnx = "1.13.8"` upstream), linked statically behind an internal `StreamingRecognizer` trait. The third-party `sherpa-rs` crate is rejected because its repository is archived.
- **Verified offline linking:** The crate's build script downloads a prebuilt inference archive when no override is set, so Mistaken builds must set `SHERPA_ONNX_ARCHIVE_DIR`/`SHERPA_ONNX_LIB_DIR` to a locally staged archive verified by size and SHA-256 (`osx-arm64-static-lib` 20,965,936 B / `9091bf16…`; `win-x64-static-MT-Release-lib` 123,206,268 B / `56ffcf3c…`). An unverified build-time binary download is a supply-chain defect.
- **No resampling crate, one rate per stream:** `sherpa-onnx/csrc/features.cc` creates a `LinearResample` internally when the fed rate differs from 16 kHz, so the microphone path needs no resampler; the same code calls `SHERPA_ONNX_EXIT(-1)` if a stream later sees a different rate, so each recognizer stream is fixed to one sample rate for its life and a format change forces a new stream.
- **Model lifecycle:** Launch checks presence by metadata only; the approved model loads lazily on first Start after every file's size and SHA-256 is verified against a compiled-in manifest copied from Spec 05's approval, then stays cached for the process lifetime. Mismatch is `model_unsupported`, absence is `model_missing`, and there is no download, no second location, and no alternate-model fallback.
- **Two bounded audio stages:** Spec 04's 100 × 20 ms capture pool (2 s) stays, and Spec 06 adds a 30 × 100 ms inference pool (3 s) fed by copying out of the capture block and recycling it immediately, so decoding can never stall capture; saturation drops newest samples and emits at most one `inference_lagging` per second while capture continues.
- **Transcript emission contract:** Segment id `mic-<sessionId>-<segmentIndex>` carries every partial and its final; partials are throttled to one per 150 ms and never empty; endpoint produces exactly one immutable final; times are audio time from fed-sample counts, not wall clock; text is emitted verbatim apart from whitespace trimming/collapsing.
- **All rewriting features off by construction:** hotwords, homophone replacement, `rule_fsts`/`rule_fars`, CTC FST decoding, punctuation/ITN models, and any word-substitution table are absent or explicitly disabled; endpoint rules are frozen at 2.4 s / 1.0 s / 15 s and verified against Spec 05's latency gates.
- **Clean cutover at Spec 06:** Production Start Listening/Stop becomes the single microphone capture action and Spec 04's temporary `Test microphone` diagnostic is removed, so no duplicate capture path or stale "transcription unavailable" copy survives.
- **macOS system-audio minimum is 13.0:** ScreenCaptureKit's `SCStream` exists from 12.3, but every audio property (`capturesAudio`, `sampleRate`, `channelCount`, `excludesCurrentProcessAudio`) is macOS 13.0+, so 12.3 would ship a build whose system-audio half cannot work. The adapter also checks the running version at start and returns an explicit unsupported error; CoreAudio process taps (14.2+) are rejected to avoid a second permission model.
- **Audio-only ScreenCaptureKit, no pixels:** `SCStream` requires a content filter, so Mistaken must request the Screen Recording permission for audio-only capture and says so plainly. Exactly one audio stream output is registered, non-audio deliveries are counted and ignored, the nominal 2×2/1 fps configuration is documented as never consumed, and no image, frame, or file is ever read or written.
- **Honest screen-recording permission:** availability is decided by the TCC-gated `getShareableContentWithCompletionHandler` result; the redacted `getCurrentProcessShareableContent…` API is banned because it returns content without user consent and would fabricate availability. A distinct restart-required state exists instead of a generic failure, and nothing prompts outside an explicit start.
- **macOS capture format:** stereo 48 kHz is requested (framework default, avoids relying on ScreenCaptureKit's undocumented resampler), the delivered format description is validated every time instead of assumed, a mid-session rate change is rejected before it can reach the recognizer, and one downmix stage produces exact 20 ms mono blocks symmetric with the microphone path.
- **Platform adapters are standalone crates:** Specs 07 and 08 ship `crates/<platform>-system-audio/` with their own manifests/lockfiles, no dependency on the app crate, Tauri, serde, or ASR, and an explicit mapping table onto Spec 03's `AudioError`/`RuntimeError` that Spec 09 implements. Required shared-file changes (for example `minimumSystemVersion: "13.0"`) are recorded and yielded to the integration owner, never edited from a platform worktree.
- **Windows minimums, two numbers:** API floor is Windows 10 1703 (build 15063), because event-driven loopback receives no events below it and Microsoft's documented workaround needs a second render stream purely for pacing — refused. Supported and tested floor is Windows 10 22H2 (build 19045) / Windows 11; builds in between run the same code and are recorded as untested. Per-process loopback is excluded with per-application capture.
- **Windows loopback has no permission gate:** render-endpoint loopback needs no prompt, no elevation, no driver, no virtual cable, and no "Stereo Mix" device, so the Windows adapter exposes no permission API and no error kind maps to `system_audio_permission_denied`. Copying the macOS permission wording to Windows is prohibited; the real effect of the desktop microphone privacy toggle is measured and recorded.
- **Honest continuous timeline on Windows:** WASAPI loopback delivers no packets while the endpoint is idle, so the adapter reconstructs time from device position, fills the measured gap with zero frames capped at 10 s, resynchronizes on cap or discontinuity, treats `AUDCLNT_BUFFERFLAGS_SILENT` as zeros without reading the buffer, and keeps `captured_frames`, `silent_flag_frames`, and `synthesized_frames` as separate counters. No report may present synthesized silence as captured audio, and synthesized silence never satisfies a "receiving" activity condition.
- **Default-endpoint change must be detected:** switching the default output while the old device still exists does not invalidate the audio client, so capture would continue on a device nobody hears. The adapter compares the default render endpoint id at most once per second and ends the session with a distinct `EndpointChanged` kind; `IMMNotificationClient` is the recorded alternative, rejected to avoid implementing a COM callback interface for one boolean.
- **Windows capture format and COM hygiene:** the endpoint mix format is requested as-is (no `AUTOCONVERTPCM`, no driver-side converter) and converted in one tested stage covering 32-bit float and 16/24/32-bit integer PCM with `WAVEFORMATEXTENSIBLE` subformat discrimination; the session rate is fixed at `Initialize`; COM is initialized MTA on the adapter's own capture thread and uninitialized there after every interface is released, never on a Tauri-owned thread.
- **Documented Windows capture limits:** DRM-protected content is not captured by a trusted driver and is indistinguishable from genuine silence, loopback carries the whole session mix rather than one application, and Remote Desktop redirects audio to session-specific devices. These are stated to users rather than treated as defects, and WASAPI offers no per-process exclusion, so any future Mistaken audio output becomes a feedback constraint for Specs 09–10.
- **Atomic dual-source start:** `startCapture` accepts microphone-only, system-only, or both, and rejects the no-source request as `invalid_request`. Every requested source must reach a playing stream with a live recognizer stream or the whole start fails and rolls back both sources, because silently transcribing one side of a conversation the user asked to capture in full produces a trustworthy-looking but incomplete transcript. Opting out is an explicit user action.
- **Total source isolation, one shared recognizer:** each source owns its platform capture, 100 × 20 ms pool, 30 × 100 ms inference stage, `OnlineStream`, worker thread, segment index, status, and counters; only the loaded recognizer is shared as `Arc` (the crate documents `Send + Sync`), so model memory is paid once and in-memory audio stays bounded at 10 s total. `decode_multiple_streams` stays unused in V1 to avoid head-of-line blocking between sources; it is a recorded Spec 12 CPU candidate.
- **Structural attribution only:** `AudioSource` travels from the platform backend through pool, stage, and worker into `TranscriptSegment.source`; nothing infers source from text, energy, timing, order, or a default, a source-mismatched block is a fatal `internal` error rather than a correction, and zero cross-attribution across scored utterances including deliberate overlaps is a blocking criterion.
- **Cross-source order is emission order:** within a source finals are non-decreasing in `endedAtMs`; across sources visible order equals finalization order through Spec 02's first-seen ordering. A 1–2 s reorder window keyed on `startedAtMs` was rejected: it delays every final, contradicts the frozen reducer contract, and cannot make genuine cross-talk linear. Timestamps come from one session clock origin plus per-source fed-frame counts so the two sources remain comparable without timestamp sorting anywhere.
- **No `- ` in native code:** the system prefix exists only in Spec 02's formatter output from `source === "system"`; a `- ` in any native transcript payload is a defect with its own acceptance criterion.
- **Honest partial degradation after start:** a source dying mid-session marks only that source as failed with its exact code and `source`, tears down only its resources, leaves the survivor transcribing, and never deletes, rewrites, or reorders finalized transcript content; `captureStatus` stays `listening` while any requested source captures and becomes `error` only when all have failed. Automatic retry and reconnect belong to Spec 10.
- **System audio is opt-in and volatile:** the toggle defaults to off and is never persisted, so no macOS screen-recording prompt is reachable without an explicit user action, and per-source error copy is keyed off codes per platform — macOS Screen Recording guidance never appears on Windows and Windows failures never borrow permission wording.
- **Bounded, visible recovery only:** transient source-local failures (Windows `EndpointChanged`/`DeviceInvalidated`/`AudioServiceDown`, macOS `StreamStopped`, microphone disconnect) retry at most 3 times per source per session with 500 ms/2 s/5 s backoff, show `Reconnecting … attempt N of 3`, and reset the budget after 60 s of healthy capture with real signal. Permission, platform-version, model, format, and `internal` failures never retry, because they are user-owned or permanent; exhaustion is terminal with the underlying reason preserved.
- **Recovery must satisfy the frozen reducer:** a recovery keeps the session id and id format, continues the per-source segment index (only a new session resets it), abandons the failing source's in-flight interim without a final, creates a fresh recognizer stream per attempt (the device rate may have changed, and reusing a stream would trip the fatal `SHERPA_ONNX_EXIT` path), and clamps the restarted offset to ≥ that source's last emitted `endedAtMs` so timestamps stay monotonic. The lost audio is simply absent — no synthesized gap, no transcript annotation.
- **Degradation is visible, never automatic:** sustained lag (>20 % dropped inference blocks in a 10 s window) enters a distinct degraded state per source and leaves it after a clean window, while `inference_lagging` stays ≤1/s per source. The app never changes thread counts, model configuration, sample rates, endpoint rules, or source enablement in response to load, so runtime behavior stays identical to what Spec 05 benchmarked and Spec 12 accepts.
- **Failures are contained, transitions are bounded:** every native worker body catches panics and converts them to a source-terminal `internal` error with full release and sanitized detail; a poisoned lock ends the session rather than reusing untrusted state; no lock is held across capture, permission, stream, join, backoff, recognizer, or emission work; `starting` is watchdog-bounded at 10 s (excluding the user-driven macOS prompt) and `stopping` at 3 s, so the UI can never sit in a transitional state.
- **One shutdown path, honestly scoped:** a single idempotent native `shutdown()` releases both sources and is invoked from the window close event, Tauri's `ExitRequested`/`Exit` handling, and `SIGINT`/`SIGTERM`/console-close, never depending on React cleanup. `SIGKILL` and power loss are stated as unhandleable rather than claimed graceful, and nothing about a failure, retry count, or device is persisted across launches.
- **Long-run trust is measured:** a 60-minute continuous dual-source soak on each reference host samples RSS, thread and handle counts, per-source counters, and audio-time drift at least every 5 minutes; RSS growth after minute 5 must stay within 5 %, thread and handle counts must be flat, and repeated recoveries must leave no allocation ratchet. The transcript is the only permitted monotonic growth.
- **Waves 6 parallel split is exact:** Spec 10 owns native lifecycle plus `src/features/audio/**`; Spec 11 owns `src/App.tsx` and `src/features/transcript/**`. Needs crossing that line are recorded and yielded to the integration owner, and neither spec may widen Spec 09's frozen event union.
- **Platform-correct, non-hijacking shortcuts:** one cleaned-up window listener handles only `Cmd/Ctrl + Enter` for Start/Stop and `Cmd/Ctrl + Shift + C` for Copy All, rejects repeat/composition/disabled cases, and calls `preventDefault()` only after handling; `Space`, native selection, platform copy/select-all, and text navigation remain untouched.
- **Follow only with user consent:** transcript auto-follow is active only within 64 px of the bottom; every user scroll beyond that threshold detaches it, and new finals, interim growth, status changes, or recovery events must leave detached `scrollTop` unchanged. Scrolling back or activating the accessible `Jump to latest` button restores following.
- **Interims stay readable but silent:** the named transcript `role="log"` keeps its implicit polite behavior for finalized rows, while each interim subtree overrides it with `aria-live="off"` so up to roughly 13 dual-source interim mutations per second cannot flood VoiceOver or Narrator. Finals are announced once without a mirrored live region; real assistive-technology evidence remains mandatory.
- **Bounded frontend lifecycle:** owned UI code may create exactly one keydown listener, one scroll listener with at most one outstanding animation frame, one reduced-motion listener, one elapsed interval while listening, and one copy-feedback timeout. Every resource has an explicit unmount/state-transition cleanup path, elapsed time is recomputed from its start instant rather than tick-counted, and reduced-motion falls back to instant scrolling.
- **Long transcripts are measured before abstracted:** with roughly 1,000 finalized segments, one interim update must rerender only its row plus the container. Virtualization, truncation, or a second transcript store is prohibited unless measured failure is recorded as a later integration-owner decision.
- **Acceptance is a serialized whole-app gate:** Spec 12 begins only after Specs 10 and 11 merge, uses one writer because measured defects may require root-cause fixes anywhere, and authorizes Specs 13/14 only from one reviewed post-acceptance SHA.
- **Offline proof requires prevention and observation:** every measured build/run has no non-loopback route, while per-process permitted and blocked attempts are traced; an attempted DNS/socket/HTTP connection fails even when offline networking prevents success.
- **Privacy is content-aware and boundary-honest:** transcript sentinel scans, browser-storage inspection, file-write traces, PCM flow review, and log scans prove no user-content persistence; unavoidable OS/WebView metadata is classified rather than denied, and explicit final-only Copy All remains the sole user-owned clipboard boundary.
- **Integrated performance uses exact gates:** real-app visible latency keeps Spec 05’s ≤900 ms median partial and ≤1500 ms p95 final thresholds; process-tree CPU/RSS use one-second samples, normal flow requires zero drops, overload must remain fixed-capacity and visible, and each host repeats a final-binary 60-minute dual soak.
- **Packaging inputs freeze at Spec 12:** version is `0.1.0`; the approved ≤120 MB model is bundled under the one `$RESOURCE/resources/models/<model_id>/` layout with shared third-party notices; model/runtime/config identities and every committed lockfile are frozen before Specs 13 and 14 branch.
- **Acceptance verdicts fail closed:** a host is `PASS` only with every applicable real-host criterion and no High/Medium finding; runnable defects are `FAIL`, external missing prerequisites are explicit `BLOCKED`, and the cross-host result passes only when both hosts pass.
- **macOS V1 distribution is Apple Silicon only:** Spec 13 emits `aarch64-apple-darwin` artifacts and requires real macOS 13.x Apple Silicon floor evidence. Intel/universal support is excluded until its runtime archive, model, size, and real-host behavior pass the same gates.
- **macOS ships as `.app` plus standard DMG:** Direct distribution outside the Mac App Store uses Tauri’s drag-to-Applications DMG. `.pkg`, App Store/TestFlight, Homebrew, custom DMG marketing art, privileged helpers, launch agents, and public upload are excluded.
- **No macOS updater path:** Tauri Updater, Sparkle, update endpoints/keys/artifacts, background checks, and differential updates remain absent; a future version is a new manually installed artifact unless a later product decision creates a separate spec.
- **Developer ID trust is explicit:** A macOS `PASS` artifact requires `Developer ID Application` signing, hardened runtime, secure timestamp, accepted `notarytool` result with inspected log, a stapled ticket, Gatekeeper assessment, and a quarantined Finder install that launches offline without bypass.
- **macOS stays non-sandboxed with an empty entitlement baseline:** Direct Developer ID V1 does not enable App Sandbox. Hardened-runtime exceptions, sandbox audio-input access, `get-task-allow`, JIT/unsigned-memory, dyld, debugger, and library-validation exceptions are absent unless a real signed-build failure proves one exact Apple-documented requirement and full review/rerun approves it.
- **macOS permission manifest is minimal:** `NSMicrophoneUsageDescription` is exactly `Mistaken uses your microphone to transcribe your speech locally on this Mac.` Apple publishes no Screen Recording purpose key, so none is guessed; ScreenCaptureKit permission truth comes from the real built flow.
- **Packaging reproducibility is semantic, not falsely byte-identical:** Two clean ad-hoc builds from one SHA must have identical normalized payload manifests; timestamped signatures, notary tickets, and DMG container metadata are closed documented exclusions. The final stapled DMG receives one immutable SHA-256 after its last mutation.
- **Packaging blockers stay narrow:** Missing Developer ID/notary credentials, Apple notary service availability, or a qualifying macOS 13.x Apple Silicon floor host may produce explicit `BLOCKED` fields only after every reachable criterion passes. Ad-hoc output is local evidence and is never distributable or a `PASS`.
- **Windows V1 distribution is x64 only:** Spec 14 emits `x86_64-pc-windows-msvc` artifacts; ARM64 and 32-bit are excluded until a benchmark, runtime archive, and real-host evidence exist for them. The declared floor stays Windows 10 22H2 build 19045 / Windows 11, with the 15063–19044 API-floor range explicitly untested.
- **Windows ships one per-user NSIS setup executable:** `Mistaken_0.1.0_x64-setup.exe` installs under `%LOCALAPPDATA%` with `installMode: currentUser`, no UAC prompt, no elevation manifest, no service, no scheduled task, no `Run`/startup key, and no driver. MSI/WiX, MSIX, Microsoft Store, winget/Chocolatey, portable ZIP, and a second installer artifact are excluded.
- **WebView2 is embedded as the Evergreen offline installer:** `webviewInstallMode: offlineInstaller` (~127 MB) is frozen so installation works with networking disconnected. `downloadBootstrapper`/`embedBootstrapper` are rejected for requiring internet at install time, `skip` for silently breaking launch, and `fixedVersion` for shipping an unpatched engine over 250 MB with UNC and Windows 10 `icacls` constraints.
- **No Windows system dependency:** `build > windows > staticVCRuntime` stays `true` and `bundleVCRuntime` stays `false`; with Spec 06's static `win-x64-static-MT-Release-lib` archive and Spec 08's no-redistributable finding, the installer adds no Visual C++ Redistributable, .NET, driver, or virtual audio device.
- **Windows packaging needs one pinned online staging phase:** the Tauri bundler fetches NSIS tooling and the WebView2 offline installer on a cold cache, so Spec 14 separates a traced, digest-pinned staging step restricted to documented Tauri/Microsoft hosts from the measured offline build instead of claiming a fully offline cold build.
- **Authenticode trust is explicit and honest:** a Windows `PASS` artifact requires SHA-256 signatures with an RFC 3161 timestamp on both the setup and application executables, a passing `signtool verify /pa /all`, and a real download-path install as a standard user. Observed SmartScreen/Defender behavior is recorded as evidence; disabling or bypassing a Windows security control, or promising a prompt-free first run, is prohibited.
- **Windows uninstall honesty:** uninstall must remove the application directory, embedded model, notices, shortcuts, and the `HKCU` uninstall entry, while the Microsoft-owned WebView2 runtime and its user-data folder are classified as retained OS state rather than falsely claimed removed.
- **Wave 8 ownership is isolated per platform:** Spec 13 owns only `src-tauri/tauri.macos.conf.json`, macOS `Info.plist`/evidenced entitlement file, `icon.icns`, and `distribution/macos/**`; Spec 14 owns only `src-tauri/tauri.windows.conf.json`, `icon.ico`, an evidenced NSIS hook file, and `distribution/windows/**`. Any frozen shared-input correction returns to the integration owner and restarts both branches from one reviewed SHA.
- **Release is one candidate, not two platform successes:** Spec 15 accepts only two artifacts provably built from one release SHA at version `0.1.0` with identical frozen inputs; differing packaging base SHAs, a stale artifact, or a payload difference outside the documented platform set is a release-blocking defect, and any fix rebuilds both artifacts and reruns every invalidated criterion on both platforms.
- **Release acceptance is serialized:** Spec 15 has one writer and no parallel peer, because it may fix defects anywhere; a concurrent change landing during the run invalidates every artifact-dependent measurement.
- **Release evidence measures the installed artifact:** every runtime criterion runs against the signed, downloaded, clean-account installation on four hosts (macOS primary and 13.x floor, Windows 11 and 10 22H2 floor) with networking disabled, including the Spec 12 corpus protocol, Spec 05 gates unchanged, and one 60-minute installed dual soak per platform. Development-build or single-platform evidence is never accepted.
- **Parity is structural, not textual:** one identical scripted conversation must yield structurally identical `Copy All` output, states, control enablement, and accessibility semantics on both platforms; recognition text may differ, structure may not, and the only allowed differences are the documented platform permission guidance and native affordances.
- **Release evidence has its own closed schema:** Spec 12's schema and scenario thresholds stay frozen and read-only, so Spec 15 adds `release/schema.json` plus `manifest.json`, `CHECKSUMS.txt`, `RELEASE_NOTES.md`, `ROLLBACK.md`, `PUBLICATION.md`, and `release/evidence/**`, with tests rejecting contradictions such as `RELEASE-READY` alongside any `FAIL`/`BLOCKED` gate, an unsigned artifact, or an open High/Medium finding.
- **Publication is gated on an explicit destination:** no distribution host, domain, or account is chosen by the project. Artifacts, checksums, notes, rollback checklist, and a local annotated `v0.1.0` tag are produced unconditionally; actual upload happens only with a user-supplied destination and credentials, otherwise publication is exactly `BLOCKED` and nothing is uploaded or pushed.
- **Rollback assumes no updater:** since neither platform ships an update mechanism, release response is withdrawal plus a replacement statement, checksum invalidation, recorded revert order, and - on identity compromise - hash/submission-id preservation, issuer revocation, and rebuilding under a new identity, with users told to remove and reinstall manually.
- **Documentation reconciliation is a release criterion:** every spec's evidence record must be filled from real runs with no `Pending` field left, every context statement must match shipped reality with each correction and reason recorded, and canonical documentation must live in the repository rather than the synced staging bundle.

## Session Notes

- Product concept: a local desktop "live transcript notepad" for English speaking sessions.
- The central quality requirement is not merely high ASR accuracy; the recognizer should also avoid masking the user's real mistakes where possible.
- Example requirement: if the user says something equivalent to `I didn't knew him`, Mistaken must not contain application-level logic that changes it to `I didn't know him`.
- ASR itself may still misrecognize speech. This must be handled through model benchmarking rather than a grammar-correction layer.
- A benchmark set should contain hard cases such as `went` vs `won't`, incorrect verb forms, fillers, repetitions, fast speech, and real system-audio playback.
- No Deepgram, Azure Speech, OpenAI transcription API, or other paid speech service should be introduced as a shortcut.
- Keep implementation scope narrow until microphone -> local ASR -> transcript works cleanly end to end.
- The staging bundle lives in iCloud Drive (`~/Library/Mobile Documents/com~apple~CloudDocs/mistaken-context`) and is mirrored to `/Users/berat/mistaken-context`; it has no version control, so a sync-driven move can make the working copy look empty mid-session. Initialize Git for the bundle, or move authoring into the dedicated repository as soon as Spec 01 runs.
- All 15 specs are now authored in the staging bundle. The next action is the single cross-spec integration review, not further spec writing; Spec 01 then imports the reviewed set into the dedicated repository.
